{
  "note": "Манифест наборов промптов. Клиент читает этот файл и подгружает каждый файл из списка как отдельный набор в выпадающем списке. На GitHub Pages работает; на file:// — нет (тогда только встроенные).",
  "files": [
    { "file": "claude-code-models.json", "label": "Claude Code — выбор модели" },
    { "file": "roles.json", "label": "Роли разработки" }
  ]
}
